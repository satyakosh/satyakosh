# claim_class — a derived view, not a sealed field

Locked 2026-07-09: no claim_class field exists in sealed records. Coarse
classification of facts (definitional / measured / derived ...) is
computable from derivation.type and is therefore a consumer view per
invariant 7 ("don't seal the derivable"). Finer grain, if ever needed,
arrives by refining derivation.type values via governance records.
